import React from 'react';

const Sidebar = (props) => {
    console.log(props.title);
    return (
        <div>
            <h1>This is sidebar</h1>
        </div>
    );
};

export default Sidebar;